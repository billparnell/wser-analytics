venv/
__pycache__/
*.pyc
.DS_Store
.ipynb_checkpoints/

# dbt
target/
dbt_packages/
logs/
.user.yml

# Derived database — rebuild with `python scripts/build_db.py`
*.duckdb
*.duckdb.wal

# Activity GPX is bulky (28 MB) and the parsed Parquet holds every field of
# every point, so that is what's committed. Drop your own GPX here and run
# scripts/parse_activities.py.
data/activities/raw/

# Regenerated by scripts/clean_activities.py in a few seconds.
data/activities/*_profile.parquet
